// Hook personalizado para realizar peticiones HTTP a la API.
// Hook reutilizable para hacer peticiones HTTP (GET, POST, PUT, DELETE) al backend.
// Hook que permite realizar solicitudes HTTP a la API del backend.
// Hook que facilita el manejo de peticiones HTTP a la API del backend.
// Hook que permite realizar solicitudes HTTP a la API del backend de una manera sencilla y reutilizable.
